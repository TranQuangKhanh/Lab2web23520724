services:
  - type: static-site
    name: jsx-without-react
    env: node
    # Install dev dependencies so tsc/vite are available during build
    installCommand: "npm ci --include=dev"
    buildCommand: "npm run build"
    publishPath: "dist"
