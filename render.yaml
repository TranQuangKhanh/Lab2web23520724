services:
  - type: static-site
    name: jsx-without-react
    env: node
    branch: main
    # Ensure devDependencies (typescript, vite) are installed during build
    installCommand: "npm ci --include=dev"
    buildCommand: "npm run build"
    publishPath: "dist"
